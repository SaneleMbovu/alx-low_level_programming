#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: this code prints all base 10 numbers that are single digits
 *
 * Return: 0 on success
 */
int main(void)
{
	int i = 0;

	while (int i = 0; i < 10; i++)
{
	printf("%d", i);
}
	return (0);
}
